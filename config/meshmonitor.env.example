# Copy to /etc/mesh-gateway/meshmonitor.env on the host (chmod 600).
# deploy.sh will NOT overwrite an existing file and refuses to deploy without one.
SESSION_SECRET=generate-with: openssl rand -base64 32
ALLOWED_ORIGINS=https://mesh-gw,https://mesh-gw.local,https://192.0.2.10,https://198.51.100.10
